# Database connection
database.driverClassName=${db.driver}
database.connectionUrl=${db.url}
database.userName=${db.user}
database.password=${db.password}
database.schema=${db.schemaName}
database.dialect=h2
